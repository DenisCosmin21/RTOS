#include "uart.h"
#include "stm32u5xx.h"
#include <stdint.h>
#include <stdarg.h>
#include <stdio.h>

// ===============================================================================
//                STM32U575ZIT6Q HARDWARE DEFINITIONS (MANUAL)
// ===============================================================================

// 1. GPDMA1 Register Map
typedef struct {
  volatile uint32_t CCR;   // Control (Enable, Reset)
  volatile uint32_t CTR1;  // Transfer Settings 1 (Ports, Inc, Width)
  volatile uint32_t CTR2;  // Transfer Settings 2 (Request ID)
  volatile uint32_t CBR1;  // Block Size (Length)
  volatile uint32_t CSAR;  // Source Address
  volatile uint32_t CDAR;  // Dest Address
  volatile uint32_t CTR3;  // Transfer Settings 3
  volatile uint32_t CBR2;  // Block Size 2
  volatile uint32_t CLLR;  // Link Register
} My_GPDMA_Channel_TypeDef;

// 2. Addresses
#define MY_GPDMA1_BASE        0x40020000UL
#define MY_GPDMA1_Channel0    ((My_GPDMA_Channel_TypeDef *)(MY_GPDMA1_BASE + 0x50UL))

// Security & Privilege Registers (Must be cleared for Non-Secure access)
#define MY_GPDMA1_SECCFGR     (*(volatile uint32_t *)(MY_GPDMA1_BASE + 0x0CUL))
#define MY_GPDMA1_PRIVCFGR    (*(volatile uint32_t *)(MY_GPDMA1_BASE + 0x10UL))

// ===============================================================================

#define UART_BUFFER_SIZE 256
#define GPIOAEN     (1U << 0)
#define UART1EN     (1U << 14)
#define GPDMA1EN    (1U << 0)

#define SYS_FREQ        4000000UL
#define APB2_CLK        SYS_FREQ
#define UART_BAUDRATE   115200

#define CR1_TE      (1U << 3)
#define CR1_UE      (1U << 0)
#define CR3_DMAT    (1U << 7)
#define ISR_TXE     (1U << 7)

#define MY_DMA_CCR_EN      (1U << 0)
#define USART1_TX_DMA_REQ_ID  25

static uint16_t compute_uart_bd(uint32_t periph_clk, uint32_t baudrate);
static void uart_set_baudrate(uint32_t periph_clk, uint32_t baudrate);
static void uart_write(int ch);
static uint8_t dma_buffer[UART_BUFFER_SIZE];

// --- 1. Cache Flush (CRITICAL) ---
// The U575 has D-Cache ON. DMA reads RAM, not Cache. We must flush.
static void my_CleanDCache(uint32_t *addr, int32_t dsize) {
    if (dsize < 0) return;
    uint32_t op_size = (uint32_t)dsize;
    uint32_t op_addr = (uint32_t)addr;
    int32_t linesize = 32;
    op_size += (op_addr & (linesize - 1U));
    op_addr &= ~(linesize - 1U);
    while (op_size > 0U) {
        *(volatile uint32_t *)0xE000EF68 = op_addr;
        op_addr += linesize;
        op_size -= (op_size >= linesize) ? linesize : op_size;
    }
    __asm volatile ("dsb 0xF":::"memory");
}

// --- 2. DMA Init (CRITICAL) ---
void dma_init(void) {
    // Enable GPDMA1 Clock
    RCC->AHB1ENR |= GPDMA1EN;

    // Wait for clock
    for(volatile int i=0; i<500; i++);

    // [MAGIC 1] UNLOCK SECURITY: Make Channel 0 Non-Secure
    // Default is 1 (Secure). Your code can't start it if this is 1.
    MY_GPDMA1_SECCFGR &= ~(1U << 0);

    // [MAGIC 2] UNLOCK PRIVILEGE: Make Channel 0 Unprivileged
    MY_GPDMA1_PRIVCFGR &= ~(1U << 0);

    // Reset Channel 0
    MY_GPDMA1_Channel0->CCR &= ~MY_DMA_CCR_EN;
}

void uart_tx_init(void)
{
    RCC->AHB2ENR1 |= GPIOAEN;

    // PA9 -> AF7 (USART1_TX)
    GPIOA->MODER &= ~(1U << 18);
    GPIOA->MODER |= (1U << 19);
    GPIOA->AFR[1] |= (1U << 4) | (1U << 5) | (1U << 6);
    GPIOA->AFR[1] &= ~(1U << 7);

    RCC->APB2ENR |= UART1EN;

    uart_set_baudrate(APB2_CLK, UART_BAUDRATE);
    USART1->CR1 = CR1_TE;
    USART1->CR3 |= CR3_DMAT; // Enable DMA Request in UART
    USART1->CR1 |= CR1_UE;

    dma_init();
}

static void uart_write(int ch){
    while(!(USART1->ISR & ISR_TXE)){}
    USART1->TDR = (ch & 0xFF);
}

// --- 3. Send Function ---
void uart_send_dma(uint8_t *data, uint16_t len)
{
    // Flush Cache
    my_CleanDCache((uint32_t*)data, len);

    // Disable Channel
    MY_GPDMA1_Channel0->CCR &= ~MY_DMA_CCR_EN;
    while(MY_GPDMA1_Channel0->CCR & MY_DMA_CCR_EN);

    // [MAGIC 3] PORT ALLOCATION
    // SAP=1 (Bit 30): Source Allocated Port 1 (Memory Bus)
    // DAP=0 (Bit 31): Dest Allocated Port 0 (Peripheral Bus)
    // SINC=1: Inc Source, DINC=0: Fixed Dest
    MY_GPDMA1_Channel0->CTR1 = (1U << 30) | // Source = RAM (Port 1)
                               (0U << 31) | // Dest = Periph (Port 0)
                               (1U << 16) | // SINC
                               (0U << 14);  // DINC

    // Request Selection (USART1_TX = 25)
    MY_GPDMA1_Channel0->CTR2 = (USART1_TX_DMA_REQ_ID << 0);

    // Block Length
    MY_GPDMA1_Channel0->CBR1 = len;

    // Addresses
    MY_GPDMA1_Channel0->CSAR = (uint32_t)data;
    MY_GPDMA1_Channel0->CDAR = (uint32_t)&USART1->TDR;

    // Clear Link Register (Direct Mode)
    MY_GPDMA1_Channel0->CLLR = 0;

    // Enable
    MY_GPDMA1_Channel0->CCR |= MY_DMA_CCR_EN;
}

int __io_putchar(int ch){
    uart_write(ch);
    return ch;
}

static void uart_set_baudrate(uint32_t periph_clk, uint32_t baudrate){
    USART1->BRR = compute_uart_bd(periph_clk, baudrate);
}

static uint16_t compute_uart_bd(uint32_t periph_clk, uint32_t baudrate)
{
    return ((periph_clk + (baudrate/2U))/baudrate);
}

void uart_printf(const char *format, ...) {
    // Wait if busy
    while(MY_GPDMA1_Channel0->CCR & MY_DMA_CCR_EN);

    va_list args;
    va_start(args, format);
    int len = vsnprintf((char*)dma_buffer, UART_BUFFER_SIZE, format, args);
    va_end(args);

    if (len > 0) {
        if (len > UART_BUFFER_SIZE) len = UART_BUFFER_SIZE;
        uart_send_dma(dma_buffer, (uint16_t)len);
    }
}
